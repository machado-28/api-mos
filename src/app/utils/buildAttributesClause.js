function builAttributesClause(attributes) {
    const attribute = attributes?.length === 0 ? undefined : attributes;
    console.log(attribute);
    return attribute

}
export default builAttributesClause;