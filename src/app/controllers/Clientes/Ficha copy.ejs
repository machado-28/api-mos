<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 10px;
        }

        .container {
            border: 1px solid #000;
            padding: 20px;
            width: 94%;
            margin: 0 10px;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .header img {
            width: 100px;
        }

        .content {
            border-top: 1px solid #000;
            border-bottom: 1px solid #000;
            padding: 20px 0;
        }

        .content div {
            margin: 10px 0;
        }

        .content hr {
            border: 0;
            border-top: 1px solid #000;
            margin: 10px 0;
        }

        .qr-code {
            text-align: center;
            margin-top: 20px;
        }

        .center-content {
            text-align: center;
            margin: 0 auto;
            width: 80%;
            /* Ajuste conforme necessário */
        }

        #logo {
            height: 30px;
            margin-right: 20px;
            margin: 6px;

        }
        .content div strong{
            margin-right: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <section class="center-content ">
            <div class="">
                <img src="http://localhost:4000/public/metalica.png" id="logo" alt="Metaloca logo">

            </div>
            <strong>Metalica oil services</strong> <br>
            <span>Construção civil e obras públicas</span>

        </section>
        <div class="center-content">
            <strong>FICHA DE PEDIDO DE VISTO DE <%= data?.tipoVisto?.nome?.toLocaleUpperCase() %> </strong> <br>

        </div>
        <div class="content">
            <div>Nome: <strong>
                    <%= data?.beneficiario?.nome %>
                </strong>
                <spa></spa>
                cod: <strong>
                    <%= data?.numero %>
                </strong>
                Passaporte Nº: <strong>
                    <%= data?.passaporteNumero %>
                </strong>
                <hr>
                Nacionalidade: <strong>
                    <%= data?.nacionalidade %>
                </strong>******
                Data de Nascimento: <strong>
                    <%=new Date().toLocaleString(data?.dataNascimento) %>
                </strong>
            </div>

            <div>Data: <%= new Date(data?.createdAt).toLocaleString() %>
            </div>
            <hr>

        </div>
        <hr>
        <div>
            Foto: <br>
            <img src="<%= data.beneficiario.avatar.url %>" alt="Foto do Cliente" width="150">
        </div>
    </div>

    <div class="qr-code">
        <img src="<%= data?.qrCodeUrl %>" alt="QR Code">
    </div>
    </div>
</body>

</html>